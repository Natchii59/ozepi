generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// To remove
model Foo {
  id  Int    @id @default(autoincrement())
  bar String
}
